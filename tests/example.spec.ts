import { AudioRecorder } from "../dist/cjs/recorder.js";

describe("poop", () => {
  it("homepage has Playwright in title and get started link linking to the intro page", async () => {
    const pooper = new AudioRecorder();

    console.log(pooper);

    expect(pooper).toBeTruthy();
  });
});
